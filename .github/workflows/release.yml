name: Android Release
on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name (e.g: 1.0.0)'
        required: true
        type: string
      release_name:
        description: 'Release name (e.g: Release 1.0.0)'
        required: true
        type: string
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    environment: Release
    permissions:
      contents: write
    steps:
      - name: Resolve release metadata
        id: meta
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            TAG="${{ github.ref_name }}"
            echo "tag_name=$TAG" >> $GITHUB_OUTPUT
            echo "release_name=Release $TAG" >> $GITHUB_OUTPUT
          else
            echo "tag_name=${{ github.event.inputs.tag_name }}" >> $GITHUB_OUTPUT
            echo "release_name=${{ github.event.inputs.release_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Decode Keystore
        run: echo "${{ secrets.SIGNING_KEY }}" | base64 -d > release.jks

      - name: Build and Sign Release APK
        env:
          KEYSTORE_FILE: ${{ github.workspace }}/release.jks
          KEY_ALIAS: ${{ secrets.ALIAS }}
          KEYSTORE_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease

      - name: Rename and Cleanup APK
        run: |
          mv app/build/outputs/apk/release/app-release.apk OpenPillReminder-${{ steps.meta.outputs.tag_name }}.apk
          rm release.jks

      - name: Generate APK Hash
        id: generate_hash
        run: |
          sha256=$(sha256sum OpenPillReminder-${{ steps.meta.outputs.tag_name }}.apk | awk '{ print $1 }')
          echo "apk_sha=$sha256" >> $GITHUB_OUTPUT
          echo "SHA-256 Hash of APK: $sha256"

      - name: Create Draft Release with APK
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.meta.outputs.tag_name }}
          name: ${{ steps.meta.outputs.release_name }}
          draft: true
          files: OpenPillReminder-${{ steps.meta.outputs.tag_name }}.apk
          body: |
            <!-- Write changelog here -->
            
            ---
            ### Security Verification
            **APK SHA-256 Hash:** `${{ steps.generate_hash.outputs.apk_sha }}`
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}